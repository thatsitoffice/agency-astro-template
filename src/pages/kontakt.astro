---
import BaseLayout from '../layouts/BaseLayout.astro';
import SectionHero from '../components/SectionHero.astro';
import ContactForm from '../components/ContactForm.astro';
import { site } from '../content/site';

const pageTitle = 'Kontakt - ' + site.name;
const pageDescription = 'Nehmen Sie Kontakt mit uns auf. Wir freuen uns auf Ihre Nachricht und beraten Sie gerne zu Ihrem Projekt.';
---

<BaseLayout
  title={pageTitle}
  description={pageDescription}
  canonicalPath="/kontakt"
>
  <SectionHero
    title="Kontakt"
    subtitle="Haben Sie Fragen oder ein Projekt im Kopf? Wir freuen uns auf Ihre Nachricht!"
  />

  <section class="section" aria-label="Kontaktinformationen">
    <div class="container">
      <div class="contact-layout">
        <div class="contact-info">
          <h2>Kontaktinformationen</h2>
          
          {site.organization.contact && (
            <div class="contact-details">
              {site.organization.contact.email && (
                <div class="contact-item">
                  <strong>E-Mail:</strong>
                  <a href={`mailto:${site.organization.contact.email}`}>
                    {site.organization.contact.email}
                  </a>
                </div>
              )}
              
              {site.organization.contact.phone && (
                <div class="contact-item">
                  <strong>Telefon:</strong>
                  <a href={`tel:${site.organization.contact.phone}`}>
                    {site.organization.contact.phone}
                  </a>
                </div>
              )}
            </div>
          )}

          {site.organization.address && (
            <div class="contact-address">
              <h3>Adresse</h3>
              <address>
                {site.organization.name}<br />
                {site.organization.address.street}<br />
                {site.organization.address.postalCode} {site.organization.address.city}<br />
                {site.organization.address.country}
              </address>
            </div>
          )}

          <div class="contact-cta">
            <h3>Kostenloses Erstgespräch</h3>
            <p>Buchen Sie direkt einen Termin für ein unverbindliches Erstgespräch.</p>
            <a href="https://calendly.com/example" target="_blank" rel="noopener noreferrer" class="btn btn-primary">
              Termin buchen
            </a>
            <p class="cta-note">Oder nutzen Sie das Kontaktformular für eine individuelle Anfrage.</p>
          </div>

          <div class="contact-map-placeholder">
            <p>Karte wird hier angezeigt</p>
            <p class="map-note">Hinweis: Für eine echte Karten-Integration können Sie z.B. Leaflet oder eine andere Lösung verwenden.</p>
          </div>
        </div>

        <div class="contact-form-wrapper">
          <h2>Nachricht senden</h2>
          <ContactForm />
        </div>
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  .contact-layout {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--spacing-3xl);
    margin-top: var(--spacing-3xl);
  }

  .contact-info h2,
  .contact-form-wrapper h2 {
    margin-bottom: var(--spacing-lg);
  }

  .contact-details {
    margin-bottom: var(--spacing-xl);
  }

  .contact-item {
    margin-bottom: var(--spacing-md);
  }

  .contact-item strong {
    display: block;
    margin-bottom: var(--spacing-xs);
    color: var(--color-text);
  }

  .contact-item a {
    color: var(--color-primary);
    text-decoration: none;
  }

  .contact-item a:hover {
    text-decoration: underline;
  }

  .contact-address {
    margin-bottom: var(--spacing-xl);
  }

  .contact-address h3 {
    margin-bottom: var(--spacing-sm);
    font-size: var(--font-size-lg);
  }

  .contact-address address {
    font-style: normal;
    color: var(--color-text-light);
    line-height: var(--line-height-relaxed);
  }

  .contact-cta {
    background-color: var(--color-bg-alt);
    padding: var(--spacing-xl);
    border-radius: var(--border-radius-lg);
    margin-bottom: var(--spacing-xl);
    text-align: center;
  }

  .contact-cta h3 {
    margin-bottom: var(--spacing-sm);
    font-size: var(--font-size-lg);
  }

  .contact-cta p {
    color: var(--color-text-light);
    margin-bottom: var(--spacing-md);
  }

  .cta-note {
    font-size: var(--font-size-sm);
    color: var(--color-text-muted);
    margin-top: var(--spacing-md);
  }

  .contact-map-placeholder {
    background-color: var(--color-bg-alt);
    border: 2px dashed var(--color-border);
    border-radius: var(--border-radius-lg);
    padding: var(--spacing-3xl);
    text-align: center;
    color: var(--color-text-muted);
  }

  .map-note {
    font-size: var(--font-size-sm);
    margin-top: var(--spacing-md);
    color: var(--color-text-muted);
  }

  @media (max-width: 968px) {
    .contact-layout {
      grid-template-columns: 1fr;
      gap: var(--spacing-xl);
    }
  }
</style>
